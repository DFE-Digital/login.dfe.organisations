<a href="/" class="link-back">Back</a>

<h1 class="heading-xlarge">Link a user</h1>
<p>Use this form to link a known invitation id to a services (through an organisation).</p>

<div class="grid-row">
    <div class="col-12">
        <form method="post" id="form-edit-service">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>"/>
            <fieldset>
                <div class="form-group">
                    <label class="form-label-bold" for="invitation_id">Invitation Id</label>
                    <div>
                        <input class="form-control" id="invitation_id" name="invitation_id" type="text" required aria-required="true"
                               value=""/>
                    </div>
                </div>
                <div class="form-group">
                    <label class="form-label-bold" for="organisation_id">Organisation</label>
                    <div>
                        <select class="form-control" name="organisation_id" id="organisation_id">
                            <% for (let i = 0; i < locals.organisations.length; i++) { %>
                            <option value="<%= locals.organisations[i].id%>"><%= locals.organisations[i].name %></option>
                            <% } %>
                        </select>
                    </div>
                </div>
                <div class="form-group">
                    <label class="form-label-bold" for="service_id">Service</label>
                    <div>
                        <select class="form-control" name="service_id" id="service_id">
                            <% for (let i = 0; i < locals.services.length; i++) { %>
                            <option value="<%= locals.services[i].id%>"><%= locals.services[i].name %></option>
                            <% } %>
                        </select>
                    </div>
                </div>
                <div class="form-group">
                    <label class="form-label-bold" for="role_id">Role</label>
                    <div>
                        <select class="form-control" name="role_id" id="role_id">
                            <option value="0">End User</option>
                            <option value="10000">Approver</option>
                        </select>
                    </div>
                </div>

                <div class="form-submit">
                    <button type="submit" class="button">Save</button>
                </div>
            </fieldset>
        </form>
    </div>
</div>